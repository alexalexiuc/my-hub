// Re-export wrapper — keeps existing finances imports working.
// New code should import Modal from '@/components'.
export { Modal as FinModalShell } from '@/components';
export type { ModalProps as FinModalShellProps } from '@/components';
