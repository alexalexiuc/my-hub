// Re-export wrapper — keeps existing finances imports working.
// New code should import DatePicker from '@/components'.
export { DatePicker as SmartDatePicker } from '@/components';
export type { DatePickerProps as SmartDatePickerProps, DateMode } from '@/components';
