# shared package — Agent Guidelines

## Utility inventory — `src/utils/`

| File                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | What's inside                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `dates.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Date/time helpers: formatting (YYYY-MM-DD, UTC, locale), timezone resolution (offset + IANA), current/relative date strings (`currentDateString`, `dateStringDaysAgo`, `localDateString`), ISO week arithmetic, month/week label formatters (`shiftMonthStr`/`formatMonthStr`, `shiftWeekStr`/`formatWeekRangeStr`), addDays/addHours/addMinutes, calendarDays, formatDayHeading, fmtDuration, formatSegmentTime; calendar utils (getMonthStart/End, startOfWeekMonday, isSameDay, toDate, startOfDay); HTML input formatters (toDateInputValue, toDateTimeLocalValue); timezone display (getTimezoneNamePart, normalizeOffset, getTimezoneBadge, getFullDateTooltip); `getCurrentWeekDays` (ISO week Mon–Sun with YYYY-MM-DD + short labels) |
| `objects.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Object helpers: `omitUndefined` (strips `undefined` props), `omitNullish` (strips `null` and `undefined` props), `trimOrNull(val)` (trims string; returns `null` for empty, `undefined` unchanged — pair with `omitUndefined`), `getEnvVar(key, default?)` (reads `process.env[key]`, throws if missing and no default provided — use this instead of `process.env` directly)                                                                                                                                                                                                                                                                                                                                                                 |
| `arrays.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Array helpers: `dedupeTrimmed(values, seen?)` — trims, drops blanks, drops case-insensitive duplicates (keeps first occurrence); pass `seen` to exclude already-known values                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `coordinates.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Geographic coordinate utilities: `validateCoords` (Zod, range checks, both-or-neither), `isFiniteCoordinate`, `hasValidLatLng`, `parseCoordinatePair` ("lat,lng" string), `parseGeoUri` (geo URI), `containsPlusCode` (Plus Code detection)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| `withBackoffRetry.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Promise retry with exponential backoff: `withBackoffRetry(fn, { retries, delay, shouldRetry })`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `portfolio.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Portfolio math (pure): `projectPortfolioGrowth(opts)` — monthly compound-growth projection series (`value_{m+1} = value_m × (1+r)^(1/12) + contribution`); `PROJECTION_HORIZON_MONTHS` (120)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `calories.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | TDEE/calorie-target calculation: `calculateBMR` (Mifflin-St Jeor), `calculateCalorieTargets` (applies goal offset, floors, caps),                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `profileToTargets` (profile + latest weight → targets; single home for the field mapping), `hasDuplicateMealSlot` (weekly-menu "one meal per slot" rule), `mealOrder(gymTime)` (display order of a day's meal slots; places pre/post-workout around the training session), `dayTargetKcal` / `dayCalorieTargets(profile, weightKg, date)` (one day's ceiling+floor including the gym-day bonus — single owner of that rule), `latestWeightKg(measurements)`, `ActivityLevelMultipliers` |
| `travel.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Trip domain helpers: `deriveTripStatus` (computes Planned/Active/Completed/Cancelled), `isTransportBookingType`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `finances.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Finance domain helpers: `getCurrencySymbol(currency)` — maps currency code to display symbol (USD→$, GBP→£, EUR→€), falls back to the code itself; `isPayeeRequired(type)` — returns whether a transaction type should involve payees (false for transfers); `formatCardLastFour(cardLastFour)` — masks each comma-separated last-4 group individually (e.g. "1234,5678" -> "•••• 1234, •••• 5678") for cards also loaded into Apple Pay/Google Pay                                                                                                                                                                                                                                                                                           |
| `logger.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Minimal logger with ISO timestamps: `logger.info/warn/error` — wraps `console.log/warn/error` with an ISO 8601 prefix. Use in worker and server startup code where no framework logger is available.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

> Before writing any utility or component, check the tables above. If the function has general purpose (string, number, date, array, etc.), it belongs here even if only one place uses it currently.

## Service inventory — `src/services/travel/`

| File                    | What's inside                                                                                                                                                                       |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `trips.ts`              | Trip CRUD + access: createTrip, getTrips, getAccessibleTrips, getTripById/Accessible, updateTrip, deleteTrip, getNextTrip, getTripBookingRanges, verifyTripOwnership/Access         |
| `bookings.ts`           | Booking CRUD + filters: addTripBooking, getTripBookings, getUpcomingTripBookings, getTripBookingById, updateTripBooking, deleteTripBooking — coordinate validation on insert/update |
| `checklist.ts`          | Checklist item CRUD: addChecklistItem, getChecklistItems, updateChecklistItem, toggleChecklistItem, deleteChecklistItem                                                             |
| `companions.ts`         | Travel companion CRUD: addTripCompanion, getTripCompanions, updateTripCompanion, deleteTripCompanion                                                                                |
| `days.ts`               | Trip day notes: upsertTripDay (supports placeIds; COALESCE preserves associations when not provided), getTripDays, deleteTripDay                                                    |
| `documents.ts`          | Document records CRUD: addTripDocument, getTripDocuments, getTripDocumentById, updateTripDocument, deleteTripDocument                                                               |
| `places.ts`             | Points of interest CRUD: addTripPlace, getTripPlaces (filterable by visited/priority), updateTripPlace (now includes lat/lng), deleteTripPlace                                      |
| `shares.ts`             | Trip sharing: listTripShares, createTripShare, deleteTripShare, deleteAllUserTripShares                                                                                             |
| `semantic.ts`           | Aggregated read queries: getTripOverview, getTripBrief, getTripTimeline, getUpcomingBookings, suggestChecklistTemplate                                                              |
| `flightData.ts`         | Flight data polling: upsertFlightData, updateFlightData, getFlightDataDueForFetch, computeNextFetchAt, backOffFlightData, setFlightDataAutoUpdate, linkBookingToFlightData          |
| `flightDataApi.ts`      | AeroDataBox API client: fetchFlightFromApi — RapidAPI, 600 req/month, requires RAPIDAPI_KEY                                                                                         |
| `flightDataApiTypes.ts` | AeroDataBox response types: AeroDataBoxFlight, FlightStatus, CodeshareStatus, FlightAirportMovementQualityEnum                                                                      |

## Service inventory — `src/services/finances/`

| File                   | What's inside                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `budgets.ts`           | Budget CRUD + access: createBudget, getUserBudgets, getUserActiveBudget, setActiveBudget, getBudgetById, getBudgetMembers, updateBudget, deleteBudget, addBudgetMember, removeBudgetMember, deleteAllUserFinanceBudgets, hasAccessToBudget, enforceBudgetAccess — membership-based access via financeBudgetMembers; Types: BudgetInsert, BudgetUpdate, UserBudget, BudgetMember                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `accounts.ts`          | Account CRUD: createAccount, getAccounts (opts: includeArchived), getAccountById, updateAccount, deleteAccount; getNetWorthHistory(userId, budgetId, limit?) — last N monthly snapshots oldest-first, returns NetWorthSnapshot[]; getAvailableBalance(userId, budgetId) — sum of included non-archived balances (liabilities subtracted); default: bank+cash included, all others excluded; getAvailabilityPreferences(userId, budgetId) — Map<accountId, include> of explicit user preferences stored in financeAccountAvailability; setAccountAvailableInclusion(userId, budgetId, accountId, include) — upserts or deletes a preference row (no-op if value matches default); deleteAllUserAvailableOverrides(userId) — bulk remove preferences; isIncludedInAvailable(type, preferredInclude) — resolves effective inclusion (preference ?? default); isDefaultIncludedInAvailable(type) / AVAILABLE_DEFAULT_INCLUDED_TYPES / LIABILITY_TYPES — domain constants; getAllAccountIds() — all account IDs (worker only); recalculateAccountBalance(accountId) — recomputes balance from openingBalance + full transaction history including corrections (for loans openingBalance = original principal); returns new value or null; `showOnWidget`/`widgetSortOrder` columns (via AccountUpdate) control a loan account's dedicated card on the finances widget and its order |
| `categories.ts`        | Group + category CRUD: createGroup/getGroups/updateGroup/deleteGroup, createCategory/getCategories (excludes archived)/getCategoryById/updateCategory/deleteCategory/archiveCategory/unarchiveCategory; `includeInSpendingBudget` column (via CategoryUpdate, default true) controls whether a category's target/spend rolls into the aggregate budget total computed by reporting.ts getBudgetProgress                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `transactions.ts`      | Transaction CRUD: addTransaction, getTransactions (opts: accountId/categoryId/payeeId/type/fromDate/toDate/includeCorrections/search/addedByUserId/limit/offset), getTransactionListItems (same opts; returns TransactionListItem[] with resolved accountName/Currency, toAccountName/Currency, categoryId/Name/Color/Icon, payeeId/Name, groupName, addedByUserId/Initials, createdAt, balances via JOIN), getTransactionListItemById (single TransactionListItem by id; null if not found), countTransactions (same opts), checkDuplicateTransaction (opts: accountId/date/amount/payeeId), getTransactionById, updateTransaction, deleteTransaction — all mutating ops update payee stats; Types: TransactionInsert, TransactionUpdate, GetTransactionsOpts, DuplicateCheckOpts, TransactionListItem                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `payees.ts`            | Payee CRUD + stats: findPayeeByNameOrAlias, upsertPayee (case-insensitive via normalizedName/aliases), resolvePayeeIdByNameOrAlias (returns id or undefined), updatePayee, getPayees (returns Payee[] with aliases/description ranked by user usage), getPayeeSummary (all-time txCount/totalSpent/totalIncome/avgSpent/firstDate/lastDate/topCategory/topAccount for one payee), deletePayee, incrementPayeeStats, decrementPayeeStats; Types: Payee, PayeeUpdate, PayeeSummary                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `exchangeRates.ts`     | Exchange-rate orchestration: getExchangeRate (in-memory promise cache + DB exact hit + external API fetch + DB persist + recent-row fallback)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `loan-amortization.ts` | Loan amortization helpers: `getMonthlyPayment(principal, monthlyRate, termMonths)` — standard payment formula; `calculateLoanAmortizationSummary(details, opts?)` — schedule-derived + hybrid payment summary using actual payment history; `getLoanBalanceSnapshotForAccount(userId, budgetId, account, opts?)` — remaining principal + amortization summary; `getLoanDisplayBalance(account, loanSnapshot)` — **pure**; resolves the display balance for an account, substituting the amortization-derived remaining principal for interest-bearing loans (raw ledger balance understates remaining principal once payments include interest) — zero-interest loans and non-loan accounts return the ledger balance as-is; `getLoanCardBalance(userId, budgetId, account, opts?)` — fetches the snapshot and resolves it via `getLoanDisplayBalance` in one call; reuse this instead of repeating the snapshot + display-balance pairing in route/UI code; `buildLoanSummary(details, paymentsCompleted, totalPaid, today)` — **pure** closed-form summary (count-based; safe to call without DB for testing); `getLoanSummaryForAccount(userId, budgetId, account, opts?)` — fetches transactions then calls buildLoanSummary; Types: LoanPaymentHistoryEntry, LoanAmortizationSummary, LoanBalanceSnapshot, LoanSummary                                                    |
| `reporting.ts`         | Read-only finance reporting aggregations: getBudgetProgress (totalBudgeted/totalSpent only count categories with includeInSpendingBudget true; per-category rows are unaffected), getCashflowSummary, getSpendingByPayee (optional categoryId filter), getSpendingAggregates, getComparison (two-period side-by-side comparison with absoluteDelta/percentDelta), getAccountsCashflow(userId, budgetId, dateFrom, dateTo, accountIds?) — Map<accountId, AccountCashflowResult> of income vs spending (expenses + categorized transfers) per account, getSavingsAndDebtFlows(userId, budgetId, dateFrom, dateTo) — SavingsAndDebtFlowsResult of net transfers (in minus out) into Goal/Tracking (savings), Investment, and Loan (debt repayment) accounts for a date range, getNetWorthSummary; AccountNetWorth now includes optional `loanSummary: LoanSummary` for loan accounts                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `monthly-plans.ts`     | Monthly plan CRUD + auto-match: getMonthlyPlan, createMonthlyPlan, checkMonthlyPlanExists, checkMonthlyPlanIsPopulated (true if plan exists AND has items or availableAmount > 0 — ignores auto-created empty plans), getOrCreateMonthlyPlan, getMonthlyPlanFull (plan+items+summary), computeSummary, updateMonthlyPlan (availableAmount and/or incomeAccountId), addPlanItem (overloaded: single → FinanceMonthlyPlanItem, array → FinanceMonthlyPlanItem[]), updatePlanItem (partial update; auto-sets isAssigned when assignedAmount ≥ amount), deletePlanItem, bulkAssignAll, copyToNextMonth (copies to next month; if next month plan is an empty auto-created placeholder, updates it in-place), doesItemMatchTransaction, syncTransactionWithPlan (syncs plan items and availableAmount on transaction create/update/delete); Types: PlanItemInsert, PlanItemUpdate, MonthlyPlanFull, MonthlyPlanSummary, PlanItemWithMeta                                                                                                                                                                                                                                                                                                                                                                                                                                            |

| `portfolio.ts` | Investment portfolio service: getPortfolio, createPortfolio, updatePortfolioSettings, getPositions, upsertPositions, deletePosition (throws if position has buys), addSupply, updateSupply, deleteSupply, getSupplies (events with per-ticker lines), getPortfolioOverview (per-position value/profit with+without fees/allocation deviations + totals; self-heals price cache), getPortfolioValueHistory (daily {date, value, invested} series with read-time forward-fill), buildPortfolioOverview/buildValueHistory (**pure**, exported for tests); Types: PortfolioSettingsUpdate, PositionUpsert, SupplyInput, SupplyWithLines, PositionOverview, PortfolioOverview, PortfolioHistoryPoint |
| `tickerPrices.ts` | Ticker EOD price cache: fetchYahooDailyCloses/parseYahooChartResponse (Yahoo unofficial chart API, keyless), fetchStooqDailyCloses/parseStooqCsv (Stooq CSV fallback), getCachedTickerPrices, getLatestTickerPrice, upsertTickerPrices (onConflictDoNothing), ensureTickerPriceHistory (fills only missing head/tail of [fromDate, today]; never throws), getActivePortfolioSymbols (worker-only, distinct symbols + earliest supply date); rows keyed by (yahooSymbol, date) in `finance_ticker_prices` — global cache, trading days only |
| `labels.ts` | Label autocomplete service: `getLabels(userId, budgetId)` — list all known label strings for the budget (alphabetical); `syncLabels(userId, budgetId, labels)` — upsert provided label strings into the autocomplete table (no-op if already present). Labels are stored as a string array directly on transaction rows; this table provides autocomplete suggestions only. |

## Service inventory — `src/services/measurements/`

| File              | What's inside                                                                                                                                                                                                                                                                      |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `measurements.ts` | Body measurement CRUD: `logMeasurement` (single insert), `logMeasurementsBatch` (batch insert with shared `createdAt` + `entrySource`), `getMeasurements` (filtered by typeKey/date/entrySource), `getLatestMeasurementsPerType`, `deleteMeasurement`, `deleteAllUserMeasurements` |

## Service inventory — `src/services/calories/`

| File               | What's inside                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `profile.ts`       | Calorie profile CRUD: `getCalorieProfile`, `upsertCalorieProfile`, `deleteCalorieProfile`, `deleteAllUserCalorieProfiles`, `generateCaloriesAutomationKey` (generates + persists 64-char hex automation API key)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `meals.ts`         | Meal log CRUD: `logMeal`, `getMeals`, `getMealsForDate`, `getMealsForDateRange`, `updateMeal`, `deleteMeal`, `deleteAllUserMeals`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `day-summaries.ts` | `getDailySummariesForRange(userId, start, end)` — zero-filled per-day `{date, kcal, protein, carbs, fat, mealCount}[]` for a date range, built on `getMealsForDateRange`; used by the Hub Calendar view                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `report-data.ts`   | Aggregated report queries: `fetchWeeklyReportCaloriesData`, `fetchMonthlyReportCaloriesData`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `weekly-menu.ts`   | Weekly menu CRUD + day-log tracking: `createWeeklyMenu` (transactional; optionally writes the week's shopping list too), `updateWeeklyMenu` (patch title/prep notes only), `getWeeklyMenus`, `getWeeklyMenu`, `getWeeklyMenuByWeek`, `addMealToMenu`, `updateWeeklyMenuMeal`, `upsertMenuMeal` (insert-or-overwrite a slot; clears its day-log), `deleteWeeklyMenuMeal`, `deleteWeeklyMenu`, `deleteAllUserWeeklyMenus`, `hasAccessToMenu`, `logMenuMeal` (transactional journal entry + day-log marker), `unlogMenuMeal` (undoes both, in one transaction), `getLoggedDays`, `getMenuStatusForRange(userId, start, end)` — per-date `{hasMenu, logged}` for a date range in 3 batch queries, used by the Hub Calendar's menu icon. No marker-only "mark as logged" helper exists — writing a day-log without its journal entry is the desync `logMenuMeal` is built to prevent. |
| `shopping-list.ts` | Weekly-menu shopping list CRUD: `getShoppingListItems`, `addShoppingListItems` (bulk append, trim + case-insensitive dedupe), `replaceShoppingListItems` (clear + insert whole list in a tx; AI-authored write path), `setShoppingListItemChecked`, `deleteShoppingListItem`, `deleteAllUserShoppingListItems`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |

## Keeping the inventory current

When you **modify or add** any file under `src/utils/` or `src/services/`:

1. Update (or add) the file-level JSDoc inventory comment at the top of the file — list every named export with a one-liner.
2. Update the relevant table in this file to reflect the change.

> **Scope:** File-level JSDoc inventory comments apply only to `packages/shared/` (utils + services) and `packages/hub/src/components/`. Do **not** add them to feature folders (`src/app/…`) in hub or mcp-server.

## Build dependency

`packages/mcp-server` and `packages/hub` both compile against `packages/shared/dist/`, **not** the TypeScript source directly.

After editing anything in `packages/shared/src/`, run:

```
cd packages/shared && npm run build
```

before type-checking or running dependent packages. Without this step, downstream packages will see stale type definitions and report false errors.

## Extension points

### `trip_bookings.details` (JSONB)

This column is the designated extension point for booking-type-specific metadata. Do not add new columns to `trip_bookings` for type-specific fields — put them in `details` instead.

Conventions:

- Define a TypeScript interface in `src/types/index.ts` for each details shape.
- Always add a `readonly kind: '<type>'` discriminator field so consumers can distinguish shapes at runtime without checking `booking_type`.
- Current shapes: `FlightDetails` (`kind: 'flight'`), `TransportDetails` (`kind: 'transport'`).

### `TripBookingTypes` constant

Lives in `src/constants/travel.ts`. When adding a new booking type, also update `BookingTypeIcon.tsx` in hub and any `switch`/`case` blocks in `coming-next-utils.ts` and `BookingsSection.tsx`.
